---
import BaseLayout from '../../layouts/BaseLayout.astro';
import MarkdownEditor from '../../components/MarkdownEditor';
import { useTranslations, buildEditorTranslations } from '../../i18n/utils';
import { FileText, Zap, Shield, Globe, Download, Eye } from 'lucide-react';

const lang = 'zh';
const t = useTranslations(lang);

const editorTranslations = buildEditorTranslations(t);
---

<BaseLayout
  title={t('site.title')}
  description={t('site.description')}
  keywords={t('site.keywords')}
>
  <!-- Hero Section -->
  <section class="bg-gradient-to-b from-secondary to-white dark:from-gray-800 dark:to-gray-900 py-20">
    <div class="container mx-auto px-4 text-center">
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-gray-900 dark:text-white mb-6">
        {t('hero.title')}
      </h1>
      <p class="text-xl md:text-2xl text-gray-600 dark:text-gray-300 mb-4">
        {t('hero.subtitle')}
      </p>
      <p class="text-lg text-gray-500 dark:text-gray-400 mb-8 max-w-3xl mx-auto">
        {t('hero.description')}
      </p>
      <a href="#editor" class="btn-primary inline-flex items-center space-x-2 text-lg px-8 py-4">
        <FileText className="w-5 h-5" />
        <span>{t('hero.cta')}</span>
      </a>
    </div>
  </section>

  <!-- Features Section -->
  <section class="py-20 bg-white dark:bg-gray-900">
    <div class="container mx-auto px-4">
      <h2 class="text-3xl md:text-4xl font-bold text-center text-gray-900 dark:text-white mb-12">
        {t('features.title')}
      </h2>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
        <!-- Live Preview -->
        <div class="bg-secondary dark:bg-gray-800 p-6 rounded-lg">
          <div class="w-12 h-12 bg-primary rounded-lg flex items-center justify-center mb-4">
            <Eye className="w-6 h-6 text-white" />
          </div>
          <h3 class="text-xl font-semibold text-gray-900 dark:text-white mb-2">
            {t('features.livePreview.title')}
          </h3>
          <p class="text-gray-600 dark:text-gray-400">
            {t('features.livePreview.description')}
          </p>
        </div>

        <!-- To PDF -->
        <div class="bg-secondary dark:bg-gray-800 p-6 rounded-lg">
          <div class="w-12 h-12 bg-primary rounded-lg flex items-center justify-center mb-4">
            <FileText className="w-6 h-6 text-white" />
          </div>
          <h3 class="text-xl font-semibold text-gray-900 dark:text-white mb-2">
            {t('features.toPdf.title')}
          </h3>
          <p class="text-gray-600 dark:text-gray-400">
            {t('features.toPdf.description')}
          </p>
        </div>

        <!-- To Word -->
        <div class="bg-secondary dark:bg-gray-800 p-6 rounded-lg">
          <div class="w-12 h-12 bg-primary rounded-lg flex items-center justify-center mb-4">
            <Download className="w-6 h-6 text-white" />
          </div>
          <h3 class="text-xl font-semibold text-gray-900 dark:text-white mb-2">
            {t('features.toWord.title')}
          </h3>
          <p class="text-gray-600 dark:text-gray-400">
            {t('features.toWord.description')}
          </p>
        </div>

        <!-- No Login -->
        <div class="bg-secondary dark:bg-gray-800 p-6 rounded-lg">
          <div class="w-12 h-12 bg-primary rounded-lg flex items-center justify-center mb-4">
            <Shield className="w-6 h-6 text-white" />
          </div>
          <h3 class="text-xl font-semibold text-gray-900 dark:text-white mb-2">
            {t('features.noLogin.title')}
          </h3>
          <p class="text-gray-600 dark:text-gray-400">
            {t('features.noLogin.description')}
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Editor Section -->
  <section id="editor" class="py-20 bg-gray-50 dark:bg-gray-800">
    <div class="container mx-auto px-4">
      <h2 class="text-3xl md:text-4xl font-bold text-center text-gray-900 dark:text-white mb-12">
        Markdown 在线编辑器
      </h2>
      <MarkdownEditor client:load translations={editorTranslations} />
    </div>
  </section>

  <!-- SEO Content Section -->
  <section class="py-20 bg-white dark:bg-gray-900">
    <div class="container mx-auto px-4 max-w-4xl">
      <div class="prose prose-lg dark:prose-invert max-w-none">
        <h2>关于 Markdown 在线预览编辑器</h2>
        <p>
          <strong>Markdown Online</strong> 是一款功能强大的免费 <strong>Markdown 在线编辑器</strong>，支持实时预览功能。
          无论您需要 <strong>Markdown 在线预览</strong>、<strong>Markdown 转 PDF</strong>、
          <strong>Markdown 转 Word</strong>、<strong>Markdown 转 HTML</strong>，还是 <strong>Markdown 转图片</strong>，
          我们的工具都能满足您的需求。
        </p>

        <h3>为什么选择我们的 Markdown 编辑器？</h3>
        <p>
          我们的 <strong>Markdown 在线预览</strong>工具完全在浏览器中运行，确保您的数据保持私密和安全。
          无需登录，不上传数据到服务器 - 只需纯粹、快速的 Markdown 编辑和即时预览。
        </p>

        <h3>Markdown Online 的核心功能</h3>
        <ul>
          <li><strong>Markdown 在线预览</strong>：在您输入时实时查看渲染效果</li>
          <li><strong>Markdown 转 PDF</strong>：一键将文档导出为 PDF 格式</li>
          <li><strong>Markdown 转 Word</strong>：即时转换为 .docx 格式</li>
          <li><strong>Markdown 转 HTML</strong>：从 Markdown 内容生成干净的 HTML</li>
          <li><strong>Markdown 转图片</strong>：导出为 JPG 或 PNG，方便分享</li>
          <li><strong>Markdown 语法</strong>：完整支持表格、代码高亮、任务列表等</li>
          <li><strong>多语言</strong>：支持中文、英文、日文、韩文界面</li>
        </ul>

        <h3>Markdown 语法支持</h3>
        <p>
          我们的编辑器支持所有标准 <strong>Markdown 语法</strong>，包括标题、列表、链接、图片、
          <strong>Markdown 表格</strong>、代码块语法高亮、<strong>Markdown 任务列表</strong>、
          引用块等更多功能。
        </p>
      </div>
    </div>
  </section>
</BaseLayout>

